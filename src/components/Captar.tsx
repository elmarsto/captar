import React from "preact/compat";
import { CaptarView } from "../CaptarView";

import {} from "./types";

interface CaptarProps {
  view: CaptarView;
}

export const Captar = ({ view }: CaptarProps) => {
  return <></>;
};
