import React from "preact/compat";
import { CaptureDisplay } from "./CaptureDisplay";

interface CaptureContainerProps {

}

export const CaptureContainer = (props: CaptureContainerProps) => {
    return <CaptureDisplay />
}